#!/bin/bash
#Creates a deployable build and deploys it to /tmp
gradle installDist
cp -r build/install/ticTacToe /tmp/sigTest
export PORT=4575
/tmp/sigTest/ticTacToe/bin/ticTacToe
sudo cp -r ./build/reports/tests/* /var/www/html/
