#!/bin/bash
#Creates a deployable build and deploys it to /tmp
gradle installDist
cp -r build/install/ticTacToe /tmp/runTest
export PORT=4575
/tmp/runTest/ticTacToe/bin/ticTacToe
sudo cp -r ./build/reports/tests/* /var/www/html/
